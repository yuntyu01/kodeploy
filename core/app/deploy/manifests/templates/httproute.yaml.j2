apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ app_name }}
  namespace: {{ tenant_id }}
  labels:
    app: {{ app_name }}
    tenant: {{ tenant_id }}
    user: "{{ user_id }}"
    managed-by: kodeploy
spec:
  parentRefs:
    - name: kodeploy
      namespace: default
      sectionName: https
  hostnames:
    - {{ app_name }}.kodeploy.com
  rules:
    - backendRefs:
        - name: {{ app_name }}
          port: {{ port }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ app_name }}-redirect
  namespace: {{ tenant_id }}
  labels:
    app: {{ app_name }}
    tenant: {{ tenant_id }}
    user: "{{ user_id }}"
    managed-by: kodeploy
spec:
  parentRefs:
    - name: kodeploy
      namespace: default
      sectionName: http
  hostnames:
    - {{ app_name }}.kodeploy.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
